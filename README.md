# 🌌 ENCELADA - Современный Онлайн-Кинотеатр

![Encelada Logo](images/logo.webp)

**ENCELADA** — это мощное и стильное веб-приложение для просмотра фильмов, сериалов и аниме с интеграцией TMDB, поддержкой Telegram-авторизации и глубокой кастомизацией интерфейса.

## ✨ Особенности

-   **📽️ Огромная библиотека**: Тысячи фильмов и сериалов благодаря TMDB API.
-   **🎨 Премиальный дизайн**: Современный интерфейс с поддержкой тем (Deep Black, Lava Lamp, Dynamic Background).
-   **🔐 Умная авторизация**: Традиционный вход по email или быстрая привязка через Telegram-бота.
-   **📂 Личная библиотека**: Списки "Смотрю", "В планах", "Просмотрено" и "Брошено".
-   **💬 Система комментариев**: Обсуждайте любимые тайтлы с другими пользователями.
-   **⚙️ Индивидуальные настройки**: Масштабирование карточек, управление безопасным контентом (18+) и многое другое.

## 🚀 Быстрый старт

### Требования
-   Node.js v16+
-   TMDB API Key (получите на [TMDB](https://www.themoviedb.org/documentation/api))

### Установка

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/your-username/encelada.git
    cd encelada
    ```

2.  Установите зависимости:
    ```bash
    npm install
    ```

3.  Настройте переменные окружения:
    Скопируйте `.env.example` в `.env` и заполните ваши ключи:
    ```bash
    cp .env.example .env
    ```

4.  Запустите сервер:
    ```bash
    npm start
    ```

Откройте `http://localhost:3000` в вашем браузере.

## ☁️ Деплой

### Рекомендуемые платформы
-   **Railway**: Отличный выбор, поддерживает Node.js и **Volumes** для сохранения данных в `data/*.json`.
-   **Render**: Аналогично Railway, поддерживает диски для персистентности.
-   **Vercel**: Не рекомендуется для текущей версии, так как Vercel использует временную файловую систему (файлы в `data/` будут удаляться при каждом деплое).

> [!TIP]
> Для серьезного использования на Vercel рекомендуется перевести хранение данных из JSON-файлов в базу данных (например, MongoDB Atlas или PostgreSQL).

> [!IMPORTANT]
> При деплое не забудьте добавить все переменные из `.env` в настройки окружения вашего хостинга.

## 🛠️ Технологии

-   **Backend**: Node.js, Express, Axios
-   **Frontend**: Vanilla JS (ES6 modules), Tailwind CSS
-   **API**: TMDB API, Telegram Bot API
-   **Data**: JSON-based storage (легко заменяется на MongoDB/PostgreSQL)

---

Создано с ❤️ для любителей кино.
